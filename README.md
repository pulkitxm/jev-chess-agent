# Jev Chess Agent

A local chess.com bot-game player. Jev selects every move from the complete legal move list. A rules library tracks the position and supplies factual move descriptions. No chess engine selects, ranks, or overrides moves.

The target opponent is Maximum (3200). Beating it is an experimental goal, not an established capability.

Implementation and live verification are in progress.
